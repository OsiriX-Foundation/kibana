# Default Kibana configuration from kibana-docker.

server.name: kibana
server.host: "kibana"
elasticsearch.hosts: http://elasticsearch:9200

#elastalert-kibana-plugin.serverHost: kibana
#elastalert-kibana-plugin.serverPort: 9000
